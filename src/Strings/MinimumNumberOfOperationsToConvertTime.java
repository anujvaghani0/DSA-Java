package Strings;

public class MinimumNumberOfOperationsToConvertTime {
    public static void main(String[] args) {

    }

    public static int convertTime(String current, String correct) {
        String currentH = current.substring(0, 2);
        String currentM = current.substring(3, 5);
        String correctH = correct.substring(0, 2);
        String correctM = correct.substring(3, 5);

        return 0;
    }
}
